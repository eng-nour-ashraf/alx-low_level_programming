#include "main.h"

/**
 * _memcpy - function
 * function that copies memory area
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i = 0;

	while (i < n)
	{
		dest[i] = src[];
		i++;
	}

	return (dest);
}
